{
  "name": "starlit-choices",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "vote:dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^7.1.5",
    "wrangler": "^4.135.0"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  }
}
